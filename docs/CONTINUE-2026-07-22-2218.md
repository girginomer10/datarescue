# CONTINUE — DataRescue audit fixes and `main` push

> **Status update (2026-07-22 23:10 +03):** This checkpoint was resumed in the
> same session. Its implementation and verification checklist is historical;
> use the newest entry in `docs/handoff.md` and the active notes under `memory/`
> for current state. Draft PR #1 remains intentionally unmerged.

**Saved:** 2026-07-22 22:18:54 +03:00 (Europe/Istanbul)

**Repository:** `/Users/omer/Developer.noindex/hackathon`

**Current branch:** `agent/fix-claude-audit`

**Current HEAD:** `96588f8b6d67886972466ce9a736ea8210258daf`

**Observed `origin/main`:** `4aa8ac063afa7ddcf4b9f0011064c373604f38b3`

**User objective:** Finish the confirmed audit fixes, commit all intended changes, fast-forward `main`, push `origin/main`, then wait for GitHub checks.

## CONTINUE instruction for the next chat

Continue from this exact working tree. **Do not reset, checkout away, discard, or overwrite the uncommitted changes.** Inspect current code first because it is authoritative. Complete the remaining verification/release steps below, then push the local result directly to `origin/main` as the user requested. Do not merge draft PR #1.

## Current working-tree state

The implementation is intentionally uncommitted. Modified areas include:

- Bootstrap/config: `.env.example`, `.python-version`, `Makefile`, `README.md`.
- Control plane: `apps/api/main.py`, `apps/api/store.py`, `apps/api/workflow.py`.
- Integrations: `packages/datahub/actions.py`, `packages/evidence/executor.py`, `packages/remediation/github.py`.
- Web honesty/accessibility: `apps/web/src/components/case-sections.tsx`, `common.tsx`, `CasePage.tsx`, `PolicyPage.tsx`, web tests.
- Regression coverage: backend endpoint/unit/API/audit tests plus new `test_control_plane_regressions.py`, `test_github_retry_safety.py`, and `test_toolchain_contract.py`.
- Handoff/memory: `docs/handoff.md` and `memory/bugfixes/audit-hardening-identity-provenance-and-retries.md`.

Run `git status --short` immediately to refresh this list. Preserve every intended file above.

## Confirmed fixes already implemented

1. Python bootstrap and every runtime/dbt command are pinned to Python 3.11; the example environment now documents the complete backend/frontend configuration surface.
2. Reused `event_id` values are duplicates only for equivalent normalized content; conflicting content returns `409`.
3. Short case IDs remain readable and deterministically widen on collision. A second detection cannot enter an existing case stream, including while that case is still `DETECTED`.
4. Long PostgreSQL candidate identifiers retain a source hint plus digest, preventing 63-byte truncation from merging gross/net schemas or overwriting evidence.
5. PostgreSQL execution fails fast without a DSN. Evidence executors explicitly declare whether they produce live evidence; replay evidence cannot close a live incident.
6. Post-deploy verification now:
   - validates the actual Git `origin` fetch URL against the configured GitHub repository;
   - fetches the trusted remote base and requires the supplied commit in that history;
   - canonicalizes short SHA input to a full commit OID and persists the full SHA;
   - compares the merged model byte-for-byte and by SHA-256 with the stored `PatchArtifact`;
   - runs dbt/reconciliation only from a detached temporary worktree at that verified commit;
   - requires fresh successful `DATAHUB_MCP` context for a live incident;
   - leaves the case at `PR_OPEN` without `DEPLOYMENT_RECORDED` on any verification failure.
7. DataHub MCL HTTP handling skips only a machine-labelled allowlist rejection; auth/server/network failures remain failed and are redelivered.
8. SSE cursors are validated before SQLite binding.
9. GitHub draft-PR creation now validates every actual fetch/push origin URL, anchors new branches to fetched remote `main`, validates the complete base-to-head range and exact patch, safely reconciles remote orphan/existing PR retries, preserves unknown local state, and cannot let cleanup failure erase an already-created PR success. Trailerless legacy PR #1 is accepted only through strict one-commit/tree/content proof.
10. The UI no longer presents cached evidence as live/current after a refresh failure or reset. Status, policy result, integrations, lineage, stages, and evidence switch to captured/stale language. Policy regions have valid accessible names.

## Verification completed before shutdown

- Final Python suite: **168 tests passed**.
- Web suite: **19/19 passed**.
- Frontend production build passed.
- Ruff passed.
- Mypy passed for 22 source files.
- `git diff --check` passed.
- Replay verification passed: 8 artifacts and hashes valid.
- Repo memory reindex/search/doctor passed and reported healthy.
- Live PostgreSQL/dbt vertical slice passed after the candidate-schema fix: gross `+3.40%` rejected, net `0.00%` selected, `8/8` tests, distinct evidence artifacts, candidate schemas cleaned.
- A full `make check` passed at the earlier 155-test checkpoint. The final GitHub and exact-merge changes were added afterward, so **run `make check` once more** before committing.

## Exact remaining steps

1. Re-run a final read-only diff audit. The previous second-pass audit was interrupted by shutdown after its original four blockers had been implemented. Focus on:
   - actual fetch and all push URL allowlisting;
   - remote-base/full-range GitHub ownership and trailerless PR #1 compatibility;
   - full-SHA exact patch verification and detached-worktree execution;
   - fresh DataHub context requirement for live incident resolution;
   - best-effort worktree cleanup behavior;
   - absence of `Current`/live claims in stale UI state;
   - circular-import/type issues around `_github_repository_from_remote_url`.
2. Run:

   ```bash
   uv run --python 3.11 ruff check .
   uv run --python 3.11 mypy apps packages
   uv run --python 3.11 pytest -q
   npm --prefix apps/web test -- --run
   npm --prefix apps/web run build
   uv run --python 3.11 python scripts/verify-replay.py
   make check
   git diff --check
   ```

3. Update the newest `docs/handoff.md` entry if the final test count/result changes. Extend the curated bugfix memory note with the exact merge-worktree and stale-provenance invariants, then run `make memory-reindex`, `make memory-doctor`, and a relevant `make memory-search`.
4. Inspect `git diff`, `git status`, and secret/generated-file boundaries. Do not commit `.env`, credentials, runtime data, dbt build output, or local memory cache files.
5. Commit the intended working tree on `agent/fix-claude-audit` (suggested message: `fix: close audit safety gaps`).
6. Prove the committed result from a fresh local clone before pushing:
   - clone the committed branch with `--no-local` into a temporary directory;
   - run `make install`;
   - confirm Python 3.11 is selected and dbt remains available;
   - run at least `make test-demo` (prefer `make check` if time permits).
7. Refresh `origin`. Ensure `origin/main` has not unexpectedly moved. Then fast-forward only:

   ```bash
   git switch main
   git merge --ff-only agent/fix-claude-audit
   git push origin main
   ```

   Never force-push or rewrite history.
8. Wait for the pushed commit's GitHub CI and Pages runs. Report their URLs and final SHA. Restore local service state afterward (`make postgres-down`; stop Colima if this session started it).

## External/live boundaries that remain after this push

- Do **not** claim the whole product is fully finished merely because these fixes and replay/local PostgreSQL proofs pass.
- Real DataHub Core/Kafka/MCP/GraphQL plus live OpenAI, automatic MCL-to-real-incident proof, explicit human merge, exact merge-SHA closure against the real incident, connected containment, genuine replay regeneration, demo video, and Devpost submission remain the separate completion roadmap.
- Draft PR #1 is open and must not be auto-merged.
